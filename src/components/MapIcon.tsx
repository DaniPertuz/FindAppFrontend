import React from 'react';
import { Image } from 'react-native';
import { styles } from '../theme/AppTheme';

const MapIcon = () => <Image source={require('../assets/FA_Color.png')} style={styles.mapIcon} />;

export default MapIcon;